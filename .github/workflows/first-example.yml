name: My first workflow

on: push

jobs: 
  first_job: 
    runs-on: ubuntu-latest
    steps:

    - name: checkout repo
      uses: actions/checkout@v4

    - name: run script
      run: |
        sudo chmod 777 sample-script.sh
        ./sample-script.sh
      
